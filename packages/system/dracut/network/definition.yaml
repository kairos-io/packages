name: "network"
category: "dracut"
version: "0.12"
# System files shipped separately as consumed by both dracut and user-space
requires:
- name: "network"
  category: "systemd-default"
  version: ">=0"